# Clean Architecture - Sơ đồ và Luồng xử lý

## 🏗️ Kiến trúc Tổng thể

```
┌──────────────────────────────────────────────────────────────┐
│                       CLIENT (Frontend)                      │
│                          Next.js                             │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ HTTP Request (JSON)
                        │
┌───────────────────────▼──────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Controllers (HTTP Handlers)               │  │
│  │  • UserController   • ProjectController  • TaskController │
│  │                                                        │  │
│  │  Trách nhiệm:                                          │  │
│  │  - Parse HTTP request                                  │  │
│  │  - Validate input format                               │  │
│  │  - Call Services                                       │  │
│  │  - Format HTTP response                                │  │
│  │  - Error handling                                      │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ DTOs (Data Transfer Objects)
                        │
┌───────────────────────▼──────────────────────────────────────┐
│                  BUSINESS LOGIC LAYER                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                  Services (NEW)                        │  │
│  │  • UserService   • ProjectService   • TaskService      │  │
│  │                                                        │  │
│  │  Trách nhiệm:                                          │  │
│  │  - Business logic và rules                             │  │
│  │  - Orchestrate repositories                            │  │
│  │  - Transaction management                              │  │
│  │  - Data transformation                                 │  │
│  │  - Authorization checks                                │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ Domain Entities
                        │
┌───────────────────────▼──────────────────────────────────────┐
│                   DATA ACCESS LAYER                          │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                   Repositories                         │  │
│  │  • UserRepository   • ProjectRepository  • TaskRepository │
│  │                                                        │  │
│  │  Trách nhiệm:                                          │  │
│  │  - CRUD operations                                     │  │
│  │  - Database queries                                    │  │
│  │  - Data mapping (Prisma ↔ Entity)                      │  │
│  │  - Transaction support                                 │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Models (Domain Entities)                  │  │
│  │  • User   • Project   • Task   • Comment   • Role      │  │
│  │                                                        │  │
│  │  Trách nhiệm:                                          │  │
│  │  - Domain logic                                        │  │
│  │  - Validation                                          │  │
│  │  - Business rules                                      │  │
│  │  - Encapsulation                                       │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        │ Prisma ORM
                        │
┌───────────────────────▼──────────────────────────────────────┐
│                   DATABASE LAYER                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL Database                       │  │
│  │  Tables: users, projects, tasks, comments, roles, etc. │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Luồng xử lý Request Chi tiết

### Ví dụ: Tạo User mới (POST /users)

```
┌─────────────┐
│   CLIENT    │  POST /users
│  (Frontend) │  { name, email, password }
└──────┬──────┘
       │
       │ 1. HTTP Request
       │
┌──────▼───────────────────────────────────────┐
│  ROUTER                                      │
│  router.post('/users', UserController.createUser)
└──────┬───────────────────────────────────────┘
       │
       │ 2. Route to Controller
       │
┌──────▼───────────────────────────────────────┐
│  CONTROLLER (UserController)                 │
│  ┌────────────────────────────────────────┐  │
│  │ createUser(req, res, next) {           │  │
│  │   // Parse request                     │  │
│  │   const dto = {                        │  │
│  │     name: req.body.name,               │  │
│  │     email: req.body.email,             │  │
│  │     password: req.body.password        │  │
│  │   };                                   │  │
│  │                                        │  │
│  │   // Call service                      │  │
│  │   const user = await                   │  │
│  │     userService.createUser(dto);       │  │
│  │                                        │  │
│  │   // Format response                   │  │
│  │   return { status: 201, data: user };  │  │
│  │ }                                      │  │
│  └────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────┘
       │
       │ 3. CreateUserDTO
       │
┌──────▼───────────────────────────────────────┐
│  SERVICE (UserService)                       │
│  ┌────────────────────────────────────────┐  │
│  │ async createUser(dto) {                │  │
│  │   // BUSINESS LOGIC                    │  │
│  │                                        │  │
│  │   // 1. Validate: Email exists?        │  │
│  │   const existing = await               │  │
│  │     userRepo.findByEmail(dto.email);   │  │
│  │   if (existing) {                      │  │
│  │     throw Error('Email exists');       │  │
│  │   }                                    │  │
│  │                                        │  │
│  │   // 2. Hash password                  │  │
│  │   const hash = hashPassword(dto.pass); │  │
│  │                                        │  │
│  │   // 3. Create user                    │  │
│  │   const user = await                   │  │
│  │     userRepo.create({                  │  │
│  │       name: dto.name,                  │  │
│  │       email: dto.email,                │  │
│  │       passwordhash: hash               │  │
│  │     });                                │  │
│  │                                        │  │
│  │   // 4. Map to Response DTO            │  │
│  │   return mapToResponseDTO(user);       │  │
│  │ }                                      │  │
│  └────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────┘
       │
       │ 4. Partial<User>
       │
┌──────▼───────────────────────────────────────┐
│  REPOSITORY (UserRepository)                 │
│  ┌────────────────────────────────────────┐  │
│  │ async create(data) {                   │  │
│  │   // DATA ACCESS                       │  │
│  │                                        │  │
│  │   // 1. Map to Prisma format           │  │
│  │   const prismaData =                   │  │
│  │     mapToPrisma(data);                 │  │
│  │                                        │  │
│  │   // 2. Execute database query         │  │
│  │   const created = await                │  │
│  │     prisma.user.create({               │  │
│  │       data: prismaData                 │  │
│  │     });                                │  │
│  │                                        │  │
│  │   // 3. Map to Domain Entity           │  │
│  │   return mapToDomain(created);         │  │
│  │ }                                      │  │
│  └────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────┘
       │
       │ 5. SQL INSERT
       │
┌──────▼───────────────────────────────────────┐
│  DATABASE (PostgreSQL)                       │
│  ┌────────────────────────────────────────┐  │
│  │ INSERT INTO users                      │  │
│  │ (id, name, email, passwordhash,        │  │
│  │  created_at, updated_at)               │  │
│  │ VALUES (...)                           │  │
│  └────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────┘
       │
       │ 6. Return User entity
       │
┌──────▼───────────────────────────────────────┐
│  Response flows back through layers          │
│  Database → Repository → Service → Controller│
└──────┬───────────────────────────────────────┘
       │
       │ 7. HTTP Response
       │
┌──────▼──────┐
│   CLIENT    │  { status: 201, data: { id, name, email } }
│  (Frontend) │
└─────────────┘
```

## 📦 Dependency Flow

```
┌─────────────────────────────────────────────┐
│         Dependencies Direction              │
│         (Inner ← Outer)                     │
└─────────────────────────────────────────────┘

Controller
    ↓ depends on
Service
    ↓ depends on
Repository
    ↓ depends on
Model (Entity)
    ↓ uses
Database (via Prisma)

❌ Repository KHÔNG biết Service
❌ Service KHÔNG biết Controller
✅ Dependency chỉ đi một chiều (inward)
```

## 🎯 Responsibility Matrix

| Layer | Biết gì? | Làm gì? | KHÔNG làm gì? |
|-------|----------|---------|---------------|
| **Controller** | Service, DTO | Parse HTTP, validate format, call service, format response | Business logic, database access |
| **Service** | Repository, Entity, DTO | Business logic, orchestration, validation | HTTP handling, database queries |
| **Repository** | Entity, Prisma | CRUD, queries, mapping | Business logic, HTTP |
| **Model** | - | Validation, domain logic | Database, HTTP, business rules |

## 🔀 Data Flow

```
┌─────────────────────────────────────────────┐
│  HTTP Request (JSON)                        │
│  { name: "John", email: "john@..." }        │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  CreateUserDTO (Interface)                  │
│  { name: string, email: string,             │
│    password: string }                       │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Partial<User> (Entity)                     │
│  { name, email, passwordhash }              │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Prisma Data                                │
│  { id: uuid, name, email, passwordhash,     │
│    created_at, updated_at }                 │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  User Entity (Class)                        │
│  new User(id, email, name, hash, ...)       │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  UserResponseDTO (Interface)                │
│  { id, name, email, createdAt }             │
│  (NO password!)                             │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  HTTP Response (JSON)                       │
│  { status: 201, message: "Success",         │
│    data: { id, name, email } }              │
└─────────────────────────────────────────────┘
```

## 🔐 Security Flow

```
Password Security:
Request → "password123"
    ↓
Service → Hash password
    ↓
Repository → Store "passwordhash"
    ↓
Database → Encrypted hash stored
    ↓
Response → NO password in response
    ↓
Client → Only receives safe data

DTOs prevent sensitive data exposure:
- CreateUserDTO: has password ✅
- UserResponseDTO: NO password ❌
- Only public data sent to client
```

## 🧪 Testing Strategy Flow

```
┌─────────────────────────────────────────────┐
│  Unit Tests - Services                      │
│  - Mock repositories                        │
│  - Test business logic                      │
│  - Test validation                          │
└──────────────┬──────────────────────────────┘
               │
┌──────────────▼──────────────────────────────┐
│  Unit Tests - Repositories                  │
│  - Mock Prisma client                       │
│  - Test data mapping                        │
│  - Test queries                             │
└──────────────┬──────────────────────────────┘
               │
┌──────────────▼──────────────────────────────┐
│  Integration Tests - Controllers            │
│  - Test HTTP requests                       │
│  - Test full flow                           │
│  - Test error handling                      │
└─────────────────────────────────────────────┘
```

## 📊 Code Organization

```
src/
├── controllers/          👉 Thin layer (HTTP only)
│   ├── UserController.ts      ~100 lines
│   ├── ProjectController.ts   ~150 lines
│   └── TaskController.ts      ~200 lines
│
├── services/            👉 Fat layer (Business logic)
│   ├── UserService.ts         ~250 lines
│   ├── ProjectService.ts      ~280 lines
│   └── TaskService.ts         ~320 lines
│
├── repositories/        👉 Medium layer (Data access)
│   ├── UserRepository.ts      ~200 lines
│   ├── ProjectRepository.ts   ~250 lines
│   └── TaskRepository.ts      ~350 lines
│
└── models/              👉 Domain entities
    ├── User.ts                ~150 lines
    ├── Project.ts             ~180 lines
    └── Task.ts                ~200 lines
```

## 🎯 Benefits Visualization

```
┌──────────────────────────────────────────────┐
│  BEFORE (Old Code)                           │
│  Controller handles everything:              │
│  ┌────────────────────────────────────────┐  │
│  │ • HTTP parsing                         │  │
│  │ • Business logic                       │  │
│  │ • Database queries                     │  │
│  │ • Response formatting                  │  │
│  │ • Validation                           │  │
│  └────────────────────────────────────────┘  │
│  Result: 😞 Hard to test, maintain, scale    │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  AFTER (Clean Architecture)                  │
│                                              │
│  Controller: HTTP only                       │
│  ┌────────────────────────────────────────┐  │
│  │ • Parse request                        │  │
│  │ • Format response                      │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  Service: Business logic                     │
│  ┌────────────────────────────────────────┐  │
│  │ • Validate rules                       │  │
│  │ • Process data                         │  │
│  │ • Orchestrate                          │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  Repository: Data access                     │
│  ┌────────────────────────────────────────┐  │
│  │ • CRUD operations                      │  │
│  │ • Queries                              │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  Result: 😊 Easy to test, maintain, scale    │
└──────────────────────────────────────────────┘
```

---

**💡 Key Takeaway:** Mỗi layer có trách nhiệm riêng biệt, dependencies đi một chiều, code clean và maintainable!
